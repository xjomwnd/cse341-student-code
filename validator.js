const customValidator = require('./validator');

// Example usage
console.log(customValidator.isAdult(20)); // true
console.log(validator.isEmail('example@example.com')); // true
console.log(validator.isStrongPassword('password123')); // false
